<section class="col-md-12 ml20">

  <% @users.each.with_index(1) do |user, i| %>
    <p><%= i %>. <%= link_to user.email, user_path(user) %><br />
      <% if current_user.admin? %>
        <p>Permission level: <%= user.role %></p>
        <%= link_to "Edit user rights", edit_user_path(user) %><br />
      <% end %>

      <%= link_to "Edit Info", edit_user_registration_path %> |
    <%= link_to "Delete", "/users/#{user.id}", method: "delete" %></p>
    <p>
      ------------
    </p>
  <% end %>

</section>
